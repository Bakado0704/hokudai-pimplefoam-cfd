## プロジェクトの概要

このリポジトリは、`OpenFOAM`（`pimpleFoam`）と `ParaView` を用いて、
室内におけるウイルスを含むエアロゾルの挙動を再現・可視化するためのCFDシミュレーション環境をまとめたものです。

- 室内の空気の流れに乗ったエアロゾル粒子の挙動をアニメーションとして可視化
- 各タイムステップにおける全粒子の \((x, y, z)\) 座標をCSV形式で出力
- 給排気口の位置・換気回数・室形状・粒子配置などの条件を変更し、さまざまな室内環境を再現

研究の引き継ぎや、建築環境分野・室内空気質／換気の検討を行う方が、なるべく迷わずに
シミュレーション環境を構築・改変できることを目的としています。

**このリポジトリはforkやcloneを行い、自由に改変して利用していただいて問題ありません。**

## Usage

環境構築が完了していることを前提に、最短でシミュレーションを実行する手順は次のとおりです。

```bash
# リポジトリを取得
git clone https://github.com/Bakado0704/hokudai-pimplefoam-cfd.git

# ディレクトリへ移動
cd hokudai-pimplefoam-cfd

# 計算実行
./Allrun
```

計算結果を削除してやり直したい場合は、以下を実行してください。

```bash
./Allclean
./Allrun
```

## Requirements（前提環境）

本リポジトリは、以下の環境を前提としています。

**【OS】**
- Windows10/11（blueCFD-Core 前提）

**【CFD】**
- `blueCFD-Core 2024`（`OpenFOAM`をWindows上で利用するため）
- `OpenFOAM`（pimpleFoam ソルバを使用）
- `ParaView`（粒子挙動の可視化および CSV エクスポート用）

**【バージョン管理】**
- `Git`（このリポジトリの取得・更新用）

## このリポジトリでできること

- **粒子挙動の可視化**:  
  `ParaView`を用いて、室内空間内のエアロゾル粒子の移動・滞留の様子を時系列で確認できます。

- **粒子データの出力**:  
  全粒子の座標をタイムステップごとのCSVとして保存し、粒子密度・滞留時間などの解析に利用できます。

## セットアップと詳細な解説

環境構築（Windows + `blueCFD` を前提とした `OpenFOAM` のインストール、`ParaView` の準備、  
本リポジトリのclone手順、`Allrun`/`Allclean`の使い方、VTK出力とCSVエクスポート手順など）については、
以下の前編記事で詳しく解説しています。

[【室内エアロゾルCFD：前編】OpenFOAM（pimpleFoam）環境構築の完全ガイド](ここに記事のURLを記載してください)

記事に沿って環境構築を行えば、`./Allrun`の実行から`ParaView`による粒子可視化、  および粒子座標CSVのエクスポートまで一通り再現できるようになっています。
