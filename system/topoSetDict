/*--------------------------------*- C++ -*----------------------------------*\
| =========                 |
| \\      /  Field         | OpenFOAM: The Open Source CFD Toolbox
|  \\    /   Operation     | Website:  https://openfoam.org
|   \\  /    And           | Version:  12
|    \\/     Manipulation  |
\*---------------------------------------------------------------------------*/
FoamFile
{
    format      ascii;
    class       dictionary;
    location    "system";
    object      topoSetDict;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

actions
(
    // Select the injection volume as a cellSet
    {
        name    roomZoneSet;
        type    cellSet;
        action  new;
        source  boxToCell;
        sourceInfo
        {
            // Reduced box for particle seeding: (0,0,0) to (6 15 2.8)
            box (0 0 0) (6 15 2.8);
        }
    }

    // Convert the cellSet to a cellZone
    {
        name    roomZone;
        type    cellZoneSet;
        action  new;
        source  setToCellZone;
        sourceInfo
        {
            set roomZoneSet;
        }
    }

    // Select ceiling center faces for outlet (0.4m x 0.4m)
    {
        name    outletFaceSet;
        type    faceSet;
        action  new;
        source  boxToFace;
        sourceInfo
        {
            // Room center at (2.5, 1.5, 2.3), select +/-0.2m in x,y near z=2.3
            box (1.85 2.35 2.79) (2.15 2.65 2.81);
        }
    }

    {
        name    outletFaceSet;
        type    faceSet;
        action  add;
        source  boxToFace;
        sourceInfo
        {
            // Room center at (2.5, 1.5, 2.3), select +/-0.2m in x,y near z=2.3
            box (1.85 4.35 2.79) (2.15 4.65 2.81);
        }
    }

    {
        name    outletFaceSet;
        type    faceSet;
        action  add;
        source  boxToFace;
        sourceInfo
        {
            // Room center at (2.5, 1.5, 2.3), select +/-0.2m in x,y near z=2.3
            box (1.85 10.35 2.79) (2.15 10.65 2.81);
        }
    }

    {
        name    outletFaceSet;
        type    faceSet;
        action  add;
        source  boxToFace;
        sourceInfo
        {
            // Room center at (2.5, 1.5, 2.3), select +/-0.2m in x,y near z=2.3
            box (1.85 12.35 2.79) (2.15 12.65 2.81);
        }
    }

    {
        name    outletFaceSet;
        type    faceSet;
        action  add;
        source  boxToFace;
        sourceInfo
        {
            // Room center at (2.5, 1.5, 2.3), select +/-0.2m in x,y near z=2.3
            box (3.85 2.35 2.79) (4.15 2.65 2.81);
        }
    }

    {
        name    outletFaceSet;
        type    faceSet;
        action  add;
        source  boxToFace;
        sourceInfo
        {
            // Room center at (2.5, 1.5, 2.3), select +/-0.2m in x,y near z=2.3
            box (3.85 4.35 2.79) (4.15 4.65 2.81);
        }
    }

    {
        name    outletFaceSet;
        type    faceSet;
        action  add;
        source  boxToFace;
        sourceInfo
        {
            // Room center at (2.5, 1.5, 2.3), select +/-0.2m in x,y near z=2.3
            box (3.85 10.35 2.79) (4.15 10.65 2.81);
        }
    }

    {
        name    outletFaceSet;
        type    faceSet;
        action  add;
        source  boxToFace;
        sourceInfo
        {
            // Room center at (2.5, 1.5, 2.3), select +/-0.2m in x,y near z=2.3
            box (3.85 12.35 2.79) (4.15 12.65 2.81);
        }
    }

    // Select eight inlet openings (ceiling)
    {
        name    inletFaceSet;
        type    faceSet;
        action  new;
        source  boxToFace;
        sourceInfo
        {
            // Thin slab around x=0 to capture that wall only
            box (1.85 0.35 2.79) (2.15 0.65 2.81);
        }
    }

    {
        name    inletFaceSet;
        type    faceSet;
        action  add;
        source  boxToFace;
        sourceInfo
        {
            // Thin slab around x=0 to capture that wall only
            box (1.85 6.35 2.79) (2.15 6.65 2.81);
        }
    }

    {
        name    inletFaceSet;
        type    faceSet;
        action  add;
        source  boxToFace;
        sourceInfo
        {
            // Thin slab around x=0 to capture that wall only
            box (1.85 8.35 2.79) (2.15 8.65 2.81);
        }
    }

    {
        name    inletFaceSet;
        type    faceSet;
        action  add;
        source  boxToFace;
        sourceInfo
        {
            // Thin slab around x=0 to capture that wall only
            box (1.85 14.35 2.79) (2.15 14.65 2.81);
        }
    }

        {
        name    inletFaceSet;
        type    faceSet;
        action  add;
        source  boxToFace;
        sourceInfo
        {
            // second x-column, y=0.35
            box (3.85 0.35 2.79) (4.15 0.65 2.81);
        }
    }

    {
        name    inletFaceSet;
        type    faceSet;
        action  add;
        source  boxToFace;
        sourceInfo
        {
            // Thin slab around x=0 to capture that wall only
            box (3.85 6.35 2.79) (4.15 6.65 2.81);
        }
    }

    {
        name    inletFaceSet;
        type    faceSet;
        action  add;
        source  boxToFace;
        sourceInfo
        {
            // Thin slab around x=0 to capture that wall only
            box (3.85 8.35 2.79) (4.15 8.65 2.81);
        }
    }

    {
        name    inletFaceSet;
        type    faceSet;
        action  add;
        source  boxToFace;
        sourceInfo
        {
            // Thin slab around x=0 to capture that wall only
            box (3.85 14.35 2.79) (4.15 14.65 2.81);
        }
    }
);

// ************************************************************************* //
